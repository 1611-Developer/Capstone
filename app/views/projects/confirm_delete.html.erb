<div class="confirm-delete-container">
  <h2>Delete Project: <%= @project.name %>?</h2>
  <p>Are you sure you want to delete this project? This action cannot be undone.</p>
  <%= button_to "Yes, Delete", project_path(@project), method: :delete, class: "project-action-link", form: { data: { turbo_confirm: "Are you sure?" } } %>
  <%= link_to "No, Cancel", dashboard_path, class: "project-action-link" %>
</div>

